var friendsAge = [15, 17, 14, 16]; // value insert

var sonaliAge = friendsAge[2]; // search specific value search

friendsAge[1] = 21; // update array

console.log(sonaliAge); //specific one person age

console.log(friendsAge); // output of hole array


var position = friendsAge.indexOf(14); // chack position if not present result is -1.

console.log(position);

var position = friendsAge.indexOf(141); // chack position if not present result is -1.

console.log(position);